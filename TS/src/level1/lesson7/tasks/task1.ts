{
    const enum OrderStatus {
        Pending = 'pending',
        Shipped = 'shipped',
        Delivered = 'delivered',
        Cancelled = 'cancelled'
    }

    function updateOrderStatus(status: OrderStatus): void {
        // ... update code here
    }
}